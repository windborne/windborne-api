name: Secret Scanning
on:
  pull_request:
  push:
    branches: [main, master]
  schedule:
    - cron: '0 9 * * 1'
jobs:
  scan:
    uses: windborne/.github/.github/workflows/secret-scan.yml@main
    secrets: inherit